# Frontend Configuration
FRONTEND_PORT=80
BACKEND_URL=http://localhost:8000

# Backend Configuration
BACKEND_PORT=8000
NODE_ENV=production

# CORS Configuration
# For production, set to your domain (e.g., https://yourgame.com)
# For development with different ports, use http://localhost:3000
# Use * to allow all origins (not recommended for production)
CORS_ORIGIN=*

# Game Configuration
MAX_PLAYERS_PER_ROOM=6
ROOM_TIMEOUT_MS=1800000
