services:
  simple-car-game:
    build: .
    container_name: simple-car-game
    ports:
      - "8000:8000"  # Node.js server (serves frontend + WebSocket)
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=8000
